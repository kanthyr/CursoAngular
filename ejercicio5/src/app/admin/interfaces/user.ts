export interface User {
    date_joined: string;
    email: string;
    id: number;
    is_active: boolean;
    last_name: string;
    username: string;
}

export interface responseApi{
    status: string;
}
